# config/database.yml
default: &default
  adapter: postgresql
  encoding: utf8
  pool: 5
  url: <%= ENV['DATABASE_URL'] %>

development:
  <<: *default
  database: library_development
  host: localhost

test:
  <<: *default
  database: library_test
  host: localhost